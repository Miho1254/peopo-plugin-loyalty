/**
 * File JavaScript mẫu cho phía frontend.
 * Có thể dùng để handle AJAX, hiệu ứng, v.v.
 */
(function ($) {
    'use strict';

    $(function () {
        // Ví dụ: thêm class nhấn nhá khi block loyalty xuất hiện.
        $('.peopo-loyalty-box').addClass('is-mounted');
    });
})(jQuery);
