/**
 * File JavaScript mẫu cho phần quản trị.
 * Sử dụng IIFE để tránh xung đột với các script khác trong dashboard.
 */
(function ($) {
    'use strict';

    $(function () {
        // Ví dụ: hiển thị thông báo trong console khi trang admin của plugin được load.
        console.log('Peopo Loyalty admin assets loaded.');
    });
})(jQuery);
